import 'package:flutter/material.dart';

const taken = Color(0xFF4CAF50); // Use for complete
const dueNow = Color(0xFFFFC107); // Use for Take now
const missed = Color(0xFFF44336); // Use for Missed
const upcoming = Color(0xFF9E9E9E); // Use for Upcoming
